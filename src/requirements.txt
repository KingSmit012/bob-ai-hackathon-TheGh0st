# All Python dependencies for the Threat Intel Correlation Assistant
# Install with: pip install -r requirements.txt

# === LLM Providers ===
ibm-watsonx-ai>=1.1.0         # IBM watsonx.ai SDK for Granite models
anthropic>=0.34.0              # Anthropic Claude SDK (fallback provider)

# === Data & CLI ===
pandas>=2.1.0                  # DataFrame operations for alert processing
click>=8.1.0                   # CLI argument parsing
python-dotenv>=1.0.0           # Load .env files for configuration
tabulate>=0.9.0                # Pretty-print tables in CLI output

# === UI ===
streamlit>=1.38.0              # Lightweight web dashboard
